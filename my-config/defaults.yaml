# -*- coding: utf-8 -*-
# vim: set syntax=yaml ts=2 sw=2 sts=2 et :

# Default user configuration.
default_user:
  username: user
  group: user

# Default template for AppVMs.
default_template:
  name: fedora-29

# Default NetVM for AppVMs.
default_netvm:
  name: sys-whonix
